# Для терминального чата: 03_first_agent/.env.example -> 03_first_agent/.env.
# Корневой .env ни один пример автоматически не загружает.
# HTTP-пример читает переменную окружения процесса (без секретов):
HTTP_EXAMPLE_BASE_URL=https://jsonplaceholder.typicode.com
