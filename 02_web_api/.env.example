# Необязательно: переменная окружения процесса API_LAB_TOKEN.
# Если не задана, demo.py генерирует временный токен в памяти.
# Файл .env автоматически не загружается. Только для локального учебного сервиса:
API_LAB_TOKEN=local-demo-placeholder
